import React from 'react';
import preloader from "../../image/preloader.svg"

export const Preloader = () => {

    return (
        <div>
            <img src={preloader}/>
        </div>
    );
};

